function AboutPage() {
  return (
    <section className="page-card" aria-labelledby="about-title">
      <p className="eyebrow">About</p>
      <h2 id="about-title">About this starter</h2>
      <p className="hero-copy">
        This foundation already includes routing, layout, linting, and testing support.
      </p>
    </section>
  )
}

export default AboutPage
